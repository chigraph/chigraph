#pragma once

#include <llvm/IR/Function.h>
#include <llvm/IR/Context.h>
#include <llvm/IR/TypeBuilder.h>

#include "chi/node_type.hpp"

namespace chi {
namespace std {


struct add_float : pure_node_type
{
	add_float() {
		
		std::vector<llvm::Type*> parameters = m_
		
		llvm::FunctionType* functype = llvm::FunctionType::get(nullptr, ))
		
		m_function = new llvm::Function()
		
	}
	
};


}
}


